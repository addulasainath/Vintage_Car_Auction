<%= simple_form_for @user_review, url:  user_reviews_path, html: { "data-turbo": false, class: "row g-3" } do |f| %>
  <% if @user_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_review.errors.count, "error") %> prohibited this user review from being saved:</h2>

      <ul>
        <% @user_review.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-12 mt-3">

    <label class="form-label">Review</label>
    <%= f.input :auction_id, as: :hidden, input_html: { value: @auction_id } %>
    <%= f.input :comment, input_html: { class: "form-control" }, label: false %>
  </div>
  <div class="col-12">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>