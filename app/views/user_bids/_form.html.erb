<%= simple_form_for @user_bid, html: { "data-turbo": false, class: "row g-3" } do |f| %>
  <% if @user_bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_bid.errors.count, "error") %> prohibited this user bid from being saved:</h2>

      <ul>
        <% @user_bid.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-12 mt-3">

    <label class="form-label">Amount</label>
    <%= f.input :auction_id, as: :hidden, input_html: { value: @auction_id } %>
    <%= f.input :amount, input_html: { class: "form-control" }, label: false %>
  </div>
  <div class="col-12">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>